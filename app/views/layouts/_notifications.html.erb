<% flash.each do |name, msg| %>
    <div class="ui <%= name == "notice" ? "success" : "negative" %> message">
    <div class="header">
        <%= name.upcase %>
    </div>
    <p> <%= msg %> </p>
    </div>
<% end %>